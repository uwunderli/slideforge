---
description: SF Dialoge — Chrome in Hub DESIGN.md; Implementierung SF-CSS
alwaysApply: true
---

# Dialogfenster (SlideForge)

**Design-Chrome:** nur `hub/docs/DESIGN.md` + Lab-Tab Dialogfenster.  
**Neue Design-Entscheidungen nur dort.**

Implementierung in diesem Repo: `.modal` / `.sf-dialog-*`, `SFDialog` in `ui-dialog.js`, Referenz-UI «Ribbon anpassen». Keine ad-hoc Dialog-Optik; Details nicht hier duplizieren.
