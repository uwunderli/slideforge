# Demo-Server (slideforge.service7.ch)
# Kopieren nach:  .deploy/ssh.env.demo
# Die Datei ssh.env.demo wird NICHT ins Git committed.

SSH_HOST=service7.ch
SSH_PORT=62022
SSH_USER=slideforge
SSH_PASS=HIER-PASSWORT-EINTRAGEN
REMOTE_ROOT=/

# Optional, falls der Host kein Standard-SFTP auf Port 22 nutzt:
# SFTP_REMOTE=sftp://service7.ch:62022
