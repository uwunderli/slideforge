# Prod: Gemeinde UNRAID 10.10 — openssh → /data/www/slideforge
# Kopieren nach: .deploy/ssh.env  (gitignored)

SSH_ALIAS=web-bkb10
REMOTE_ROOT=/data/www/slideforge
PROD_URL=https://slides.bkbiel.ch

# Optional explizit (wenn kein ~/.ssh/config-Alias):
# SSH_HOST=192.168.10.10
# SSH_PORT=2222
# SSH_USER=admin
# SSH_KEY_FILE=~/.ssh/id_ed25519_web_wun

# Legacy SFTP (alter green.ch-Webspace — nicht mehr für Prod):
# SSH_HOST=ftp.bkbiel.ch
# SSH_USER=slideforge
# SSH_PASS=…
