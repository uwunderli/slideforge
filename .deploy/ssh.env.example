# Copy to ssh.env and fill in your deployment credentials.
# ssh.env is listed in .gitignore and must never be committed.

SSH_HOST=ftp.example.com
SSH_USER=your-user
SSH_PASS=your-password
REMOTE_ROOT=/
