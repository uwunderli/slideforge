# SlideForge v2.1.14

**Basis:** v2.1.13 · `ASSET_VERSION` 593

## Changes

- **Launcher:** Hub-Erkennung robuster (Hub-Cookie / `auth_via`); lokal wirklich nur ein CF-Logo
- **Dialoge:** Float-Upgrade lazy + ohne DOM-Umhängen; Backdrop transparent, Fenster positioniert sich selbst

## Deploy

Prod + Demo via Pre-Push auf `main`.
